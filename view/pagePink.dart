import 'package:flutter/material.dart';

class pagePink extends StatelessWidget {
  const pagePink({super.key});

  @override
  Widget build(BuildContext context) {
    return 
    Container(
      color: Colors.pinkAccent,
    );
  }
}