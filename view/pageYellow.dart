import 'package:flutter/material.dart';

class pageYelllow extends StatelessWidget {
  const pageYelllow({super.key});

  @override
  Widget build(BuildContext context) {
    return 
    Container(
      color: Colors.yellowAccent,
    );
  }
}